// On ajoute de ClusterGroup
var GroupMarkersMap1030 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 45,
    singleMarkerMode: false,
    polygonOptions: { color: 'red' },
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_ALL = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 45,
    singleMarkerMode: false,
    polygonOptions: { color: 'red' },
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupPolygonsMap1030 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 45,
    singleMarkerMode: false,
    polygonOptions: { color: 'red' },
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupPolygonsMap1030_ALL = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 45,
    singleMarkerMode: false,
    polygonOptions: { color: 'red' },
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster05 digits-' + digits,
            iconSize: null
        });
    }
});


var GroupMarkersMap1030_99 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 18,
    maxClusterRadius: 50,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster45 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_00 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 18,
    maxClusterRadius: 50,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster45 digits-' + digits,
            iconSize: null
        });
    }
});

// Cluster Group Marqueurs par Année 
var GroupMarkersMap1030_Q99 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q01 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q02 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_02 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q03 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q04 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q05 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q06 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_06 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q07 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_07 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q08 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_08 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q09 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q10 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_02 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q11 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q12 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q13 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q14 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_06 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q15 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_07 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q16 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_08 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q17 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q18 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_02 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q19 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q20 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q21 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q22 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_06 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q99 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

// Cluster Group Marqueurs par Année - Variante B
var GroupMarkersMap1030_Q99B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q01B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q02B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_02 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q03B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q04B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q05B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q06B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_06 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q07B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_07 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q08B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_08 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q09B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q10B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_02 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q11B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q12B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q13B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q14B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_06 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q15B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_07 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q16B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_08 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q17B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q18B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_02 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q19B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q20B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q21B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_05 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Q22B = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster02_06 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_Begin = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster04 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersMap1030_End = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 100,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster03 digits-' + digits,
            iconSize: null
        });
    }
});

// ++++++++++++++++++++++++++++ others not used ++++++++++++
var GroupMarkersEntrave1030 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 35,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersSTIB1030 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 35,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster01 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersParkingPrive1030 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 35,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster03 digits-' + digits,
            iconSize: null
        });
    }
});

var GroupMarkersParking1030 = L.markerClusterGroup({
    spiderfyOnMaxZoom: true,
    showCoverageOnHover: true,
    zoomToBoundsOnClick: true,
    removeOutsideVisibleBounds: true,
    disableClusteringAtZoom: 17,
    maxClusterRadius: 35,
    singleMarkerMode: false,
    iconCreateFunction: function (cluster) {
        var digits = (cluster.getChildCount() + '').length;
        return L.divIcon({
            html: cluster.getChildCount(),
            className: 'cluster03 digits-' + digits,
            iconSize: null
        });
    }
});